import { InputStream, Lexer } from 'antlr4'

export default class <%= className %> extends Lexer {
  static readonly channelNames: string[]
  static readonly modeNames: string[]
  static readonly literalNames: string[]
  static readonly symbolicNames: string[]
  static readonly ruleNames: string[]
  static readonly grammarFileName: string

  constructor(input: InputStream)
<%_ _.each(ruleNames, (rule) => { %>
  static readonly <%= rule %>: number
<%_ }) %>
  static EOF: number
}
